var score,round,actPlayer,dice;

var score=[0,0];
round=0;
actPlayer=0;

dice = Math.floor(Math.random() *6)+1;
document.querySelector('#current-'+ actPlayer).textContent=dice;

document.querySelector()